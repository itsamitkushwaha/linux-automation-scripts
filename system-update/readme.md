# System Update Script

## Description
This Bash script updates the package list and upgrades all installed packages on Ubuntu using the `apt` package manager.

## Features

- Updates package index
- Upgrade installed packages
- Basic error handling
- User-friendly output

## Requirements

- Ubuntu/Debian Linux
- Bash
- sudo privileges

## Usage

Make the script executable:

```
chmod +x system_update.sh
```